<%- include("partials/header")  -%> 
    <h1 class="home">Home</h1>
    <p class="hcontent"><%= homec %></p>
    <% posts.forEach(post => { %>
        <h1 class="title"><%= post.title %></h1>
        <p class="content"><%= post.content.substring(0,100)+"..." %><a href= "/posts/<%=post._id%>" >Read More</a></p>
    <% }); %> 
<%- include("partials/footer")  -%> 